<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.first_activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.First_activity">
        <activity android:name=".cpage"></activity>
        <activity android:name=".maps" />
        <activity android:name=".admins" />
        <activity android:name=".ghostel" />
        <activity android:name=".bhostel" />
        <activity android:name=".hostel" />
        <activity android:name=".Health" />
        <activity android:name=".food" />
        <activity android:name=".laundry" />
        <activity android:name=".faculty_s1" />
        <activity android:name=".screen3" />
        <activity android:name=".secscreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>